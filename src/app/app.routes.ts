import { Routes } from '@angular/router';
import {RecettesListComponent} from './components/recettes/recettes-list/recettes-list.component'

export const routes: Routes = [
  { path: 'recettes', component: RecettesListComponent}
];
